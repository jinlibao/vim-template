/**
 * %FFILE%
 * Author: %USER%
 * Date: %FDATE%
 */

#include <bits/stdc++.h>

using namespace std;

#define debug(x) cerr << #x << " = " << x << '\n';

static const auto _ = []() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.precision(10);
  cout << fixed;
  return 0;
}();

void solve() {
  %HERE%
  cout << '\n';
}

int main(int argc, char* argv[]) {
  int T;
  cin >> T;
  for (int t = 1; t <= T; ++t) {
    solve();
  }
  return 0;
}
